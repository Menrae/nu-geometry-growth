# Configs

Cobaya YAML run configs live here. All MCMC runs must be defined by a file in this
directory — no ad hoc runs (see `CLAUDE.md`).

Naming convention: `<model>_<datasets>.yaml`, e.g. `lcdm_mnu_desi_pantheon.yaml`,
`w0wacdm_mnu_desi_pantheon.yaml`.
