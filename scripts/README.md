# Scripts

Launch scripts (for `cobaya-run` on configs in `configs/`) and post-processing scripts
(diagnostics, geometry/growth split, plotting).

Any run expected to take longer than 10 minutes is not launched here directly — the
launch command is printed for the user to run themselves in `tmux` (see `CLAUDE.md`).
